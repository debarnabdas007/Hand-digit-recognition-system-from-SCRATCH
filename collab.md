# Hand-digit-recognition-system-from-SCRATCH
Build on MNSIT dataset 


## NOTE:- 
as I have used tqdm which injected some hidden JavaScript/HTML "widget" metadata into that JSON file so it could animate the bar. But GitHub tries to render it as a static webpage. It sees the word "widget" in the metadata, panics because it doesn't have the animation files, and throws that exact error: the 'state' key is missing.

So, had to delete the cell outputs so that the animations do not create any problem..


### *Solution:* Open in Google Collab even CPU is enough to run this.. may connect to GPU T4 to observe completely !!
